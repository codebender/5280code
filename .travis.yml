language: ruby
rvm:
  - 2.1.2
bundler_args: --without deployment
cache: bundler
before_script:
  - cp config/database.travis.yml config/database.yml
  - psql -c 'create database code5280_test;' -U postgres
  - bundle exec rake db:test:load
branches:
  only:
    - master
env:
  - SECRET_KEY_BASE=FakeSecretKeyBaseForTestingOnlyAnd30PlusCharsLong
